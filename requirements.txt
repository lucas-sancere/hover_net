tensorflow-gpu==1.12.0
tensorpack==0.9.0.1
scikit-image==0.14.2
matplotlib===3.0.2
numpy==1.15.4
scipy==1.4.1
opencv-python==4.1.2.30
openslide-python==1.1.2

#After installing this do: conda install -c anaconda cudatoolkit=9.0 cudnn=7 

#This file is not the original one (because original was not working). To see the original one, go to original repo (not forked).Do: conda install -c anaconda cudatoolkit=9.0 cudnn=7
